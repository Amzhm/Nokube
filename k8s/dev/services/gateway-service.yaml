apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: nokube-dev
  labels:
    app: api-gateway
    tier: gateway
spec:
  selector:
    app: api-gateway
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP